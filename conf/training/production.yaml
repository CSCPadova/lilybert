# @package _global_
defaults:
  - default

# Production configuration - overrides default
num_train_epochs: 5
per_device_train_batch_size: 8
per_device_eval_batch_size: 8
gradient_accumulation_steps: 2
max_sequence_length: 1024
learning_rate: 3e-5
warmup_steps: 500
lora_r: 32
lora_alpha: 64
early_stopping_patience: 5
