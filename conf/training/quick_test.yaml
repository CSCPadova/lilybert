# @package _global_
defaults:
  - default

# Quick test configuration - overrides default
num_train_epochs: 1
per_device_train_batch_size: 2
per_device_eval_batch_size: 2
gradient_accumulation_steps: 1
max_sequence_length: 256
eval_steps: 50
save_steps: 50
logging_steps: 10
warmup_steps: 10
lora_r: 8
lora_alpha: 16
report_to: []  # Disable wandb for testing
